---
import NumberedHeading from './NumberedHeading.astro'
import SkillList from './SkillList.astro'
import { Image } from 'astro:assets'
import { aboutContent } from '@/content/about'
import profileImageSrc from '../../public/images/profile.jpg'

const { skills, description, profileImage, currentWork, previousWork } =
  aboutContent
---

<section
  id='about'
  class='py-8 lg:py-32'
>
  <div class='container mx-auto px-4'>
    <NumberedHeading
      number='01.'
      title='About Me'
      >About Me</NumberedHeading
    >

    <div class='grid grid-cols-1 md:grid-cols-[3fr,2fr] gap-12'>
      <div class='space-y-6 text-lg font-extralight text-muted-foreground'>
        {description.map((paragraph) => <p>{paragraph}</p>)}

        <p>
          Fast-forward to today, and I've had the privilege of working at{' '}
          {
            previousWork.map((work, index) => (
              <>
                <a
                  href={work.url}
                  target='_blank'
                  rel='noopener noreferrer'
                  class='text-[--links] font-normal hover:underline'
                  aria-label={work.company}
                >
                  {work.company}
                </a>
                {index < previousWork.length - 1 && ', '}
              </>
            ))
          }
          .
        </p>
        <p>
          Currently, I'm focused on building innovative web experiences at{' '}
          <a
            href={currentWork.url}
            target='_blank'
            rel='noopener noreferrer'
            class='text-[--links] font-normal hover:underline'
            aria-label={currentWork.company}
          >
            {currentWork.company}
          </a>
          , on groundbreaking projects in VR/AR and WebGL.
        </p>
        <p>Here are a few technologies I've been working with recently:</p>
      </div>
      <div class='relative w-full max-w-[380px]'>
        <div class='relative group'>
          <div
            class='absolute inset-0 transform translate-x-4 translate-y-4 border-2 border-pacific rounded transition-transform duration-300 group-hover:translate-x-6 group-hover:translate-y-6'
          >
          </div>
          <Image
            src={profileImageSrc}
            width={300}
            height={300}
            alt={profileImage.alt}
            class='relative z-10 w-full h-full rounded-lg object-cover mix-blend-darken dark:mix-blend-lighten filter grayscale contrast-100 transition-all duration-300 group-hover:filter-none group-hover:-translate-x-2 group-hover:-translate-y-2'
          />
        </div>
      </div>
      <SkillList skills={skills} />
    </div>
  </div>
</section>
