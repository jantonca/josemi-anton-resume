---
interface Props {
  skills: string[]
}

const { skills } = Astro.props
---

<ul class='grid grid-cols-2 gap-x-8 gap-y-2 mt-0'>
  {
    skills.map((skill) => (
      <li
        id={skill}
        class="relative pl-6 before:content-['▹'] before:absolute before:left-0 before:text-pacific"
      >
        {skill}
      </li>
    ))
  }
</ul>
